# CLAUDE.md

이 저장소는 **코덱스(Codex)와 Claude Code를 번갈아** 쓴다. 한쪽 한도가 떨어지면 다른 쪽으로 이어간다.

## 매 세션 반드시

1. **`HANDOFF.md`를 먼저 읽어라** — 현재 진행 상태와 다음 할 일이 들어있다.
2. **`AGENTS.md`를 읽어라** — 빌드/배포/환경변수 규칙(잘 안 변하는 것).
3. **작업에 변화가 생길 때마다 `HANDOFF.md`를 즉시 갱신하라** — 세션이 언제 끊길지 모르므로 그때그때 기록한다. (마지막에 몰아 쓰지 말 것)

자세한 규칙은 모두 `AGENTS.md`에 있다.
