{
  "version": 2,
  "installCommand": "corepack pnpm install --no-optional --no-frozen-lockfile",
  "functions": {
    "api/parse.ts": {
      "maxDuration": 60,
      "excludeFiles": "{node_modules/**/onnxruntime-node/**,node_modules/**/sharp/**}"
    }
  }
}
