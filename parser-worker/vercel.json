{
  "version": 2,
  "installCommand": "corepack pnpm install --no-optional --no-frozen-lockfile",
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 60,
      "excludeFiles": "{**/onnxruntime-node/**,**/sharp/**,**/libonnxruntime*.so}"
    }
  }
}
